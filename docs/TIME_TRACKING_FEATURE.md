# ⏱️ Time Tracking Feature

**Статус:** ✅ 100% готово!  
**Дата завершения:** 9 декабря 2025

---

## 🎯 Что это?

Time Tracking - система отслеживания времени выполнения задач с таймером, историей и статистикой.

**Основные возможности:**
1. **Start/Stop Timer** - запуск/остановка таймера для задачи
2. **History** - история всех рабочих сессий
3. **Statistics** - статистика по времени (день/неделя/месяц)
4. **Manual Entry** - ручное добавление времени

---

## ✅ Что реализовано:

### Backend (100%) ✅
- ✅ **Модель TaskTimeLog**
  - Поля: task_id, user_id, start_time, end_time, duration, notes
  - Автоматический расчет duration при остановке
  - Миграция: `2025_12_09_0310-create_task_time_logs_table.py`

- ✅ **API Endpoints**
  - `POST /tasks/{id}/time/start` - запустить таймер
  - `POST /tasks/{id}/time/stop` - остановить таймер
  - `GET /tasks/{id}/time/logs` - история времени для задачи
  - `GET /tasks/time/stats` - статистика (сегодня/неделя/месяц)
  - `POST /tasks/{id}/time/logs` - ручное добавление времени
  - `DELETE /time-logs/{id}` - удалить запись времени

- ✅ **Автоматическая остановка** активного таймера при запуске нового
- ✅ **Индексы БД** для быстрых запросов

### Frontend (100%) ✅
- ✅ **TaskTimeLogModel** (freezed + json_serializable)
- ✅ **TaskTimeRemoteDataSource** для работы с API
- ✅ **TaskTimerWidget** - красивый таймер виджет
  - Real-time обновление каждую секунду
  - Визуальная индикация активного состояния
  - Start/Stop кнопки
  - Форматирование времени (HH:MM:SS)

- ✅ **TimeLogsPage** - история времени
  - Список всех рабочих сессий
  - Общее время по задаче
  - Удаление записей
  - Pull-to-refresh

- ✅ **Интеграция в TaskDetailsPage**
  - Таймер отображается на странице задачи
  - Автоматическое обновление при изменении состояния

- ✅ **Helper extensions** для форматирования:
  - `toFormattedDuration()` - "2h 30m" или "45m"
  - `toTimeFormat()` - "2:30:45" или "30:45"

---

## 🚀 Как использовать:

### 1. Применить миграцию БД

```bash
cd backend
docker-compose exec backend alembic upgrade head
```

### 2. Использовать в приложении

**В Flutter:**
1. Откройте любую задачу (Task Details)
2. Увидите Timer Widget с кнопкой "Play"
3. Нажмите Play → таймер запустится
4. Таймер идет в реальном времени
5. Нажмите Stop → время сохранится

---

## 📊 Структура данных:

### TaskTimeLog (БД)

```sql
CREATE TABLE task_time_logs (
    id UUID PRIMARY KEY,
    task_id UUID NOT NULL REFERENCES tasks(id) ON DELETE CASCADE,
    user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    start_time TIMESTAMP WITH TIME ZONE NOT NULL,
    end_time TIMESTAMP WITH TIME ZONE,  -- NULL if active
    duration INTEGER,  -- Duration in seconds
    notes VARCHAR(500),
    created_at TIMESTAMP WITH TIME ZONE NOT NULL,
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL
);

-- Indexes for performance
CREATE INDEX idx_task_time_logs_task_id ON task_time_logs(task_id);
CREATE INDEX idx_task_time_logs_user_id ON task_time_logs(user_id);
CREATE INDEX idx_task_time_logs_start_time ON task_time_logs(start_time);
CREATE INDEX idx_task_time_logs_end_time ON task_time_logs(end_time);
```

### JSON Response Example

```json
{
  "id": "123e4567-e89b-12d3-a456-426614174000",
  "task_id": "234e5678-e89b-12d3-a456-426614174001",
  "user_id": "345e6789-e89b-12d3-a456-426614174002",
  "start_time": "2025-12-09T10:00:00Z",
  "end_time": "2025-12-09T11:30:00Z",
  "duration": 5400,  // 1.5 hours in seconds
  "notes": "Completed main feature",
  "is_active": false,
  "created_at": "2025-12-09T10:00:00Z",
  "updated_at": "2025-12-09T11:30:00Z"
}
```

---

## 🎨 UI/UX:

### Timer Widget
```
┌─────────────────────────────────────────┐
│  [⏱️]  2:30:45     [■ Stop]             │
│         Идет отсчет...                   │
└─────────────────────────────────────────┘
```

**Активное состояние:**
- Градиентный фон (primary → accent)
- Пульсирующая граница
- Время обновляется каждую секунду
- Красная кнопка Stop

**Неактивное состояние:**
- Серый фон
- Время 0:00
- Зеленая кнопка Play

### Time Logs History
```
┌─────────────────────────────────────────┐
│  Задача: "Разработка фичи"               │
│  ⏱️ Всего: 5h 30m                        │
├─────────────────────────────────────────┤
│  [✓]  2h 15m                             │
│       9 Dec 2025, 14:00                  │
│       Completed API endpoints            │
├─────────────────────────────────────────┤
│  [✓]  1h 30m                             │
│       9 Dec 2025, 10:00                  │
│       Started frontend work              │
└─────────────────────────────────────────┘
```

---

## 📈 API Примеры:

### Start Timer

```bash
curl -X POST "http://localhost:8000/api/v1/tasks/{task_id}/time/start" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"notes": "Starting work on feature"}'

# Response:
{
  "id": "...",
  "task_id": "...",
  "start_time": "2025-12-09T10:00:00Z",
  "end_time": null,
  "duration": null,
  "is_active": true,
  "notes": "Starting work on feature"
}
```

### Stop Timer

```bash
curl -X POST "http://localhost:8000/api/v1/tasks/{task_id}/time/stop" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"notes": "Completed API work"}'

# Response:
{
  "id": "...",
  "task_id": "...",
  "start_time": "2025-12-09T10:00:00Z",
  "end_time": "2025-12-09T12:30:00Z",
  "duration": 9000,  // 2.5 hours
  "is_active": false,
  "notes": "Completed API work"
}
```

### Get Statistics

```bash
curl -X GET "http://localhost:8000/api/v1/tasks/time/stats?period=month" \
  -H "Authorization: Bearer $TOKEN"

# Response:
{
  "total_time_today": 7200,    // 2 hours
  "total_time_week": 28800,    // 8 hours
  "total_time_month": 144000,  // 40 hours
  "active_task_id": "...",     // Currently active task (or null)
  "tasks": [
    {
      "task_id": "...",
      "task_title": "Feature development",
      "total_duration": 18000,  // 5 hours
      "session_count": 3,
      "average_session": 6000,  // 1.67 hours avg
      "last_worked": "2025-12-09T14:00:00Z"
    }
  ]
}
```

---

## 🔄 Workflow:

```
1. Пользователь открывает Task Details
   ↓
2. Видит Timer Widget (0:00, кнопка Play)
   ↓
3. Нажимает Play
   ↓
4. API: POST /tasks/{id}/time/start
   - Останавливает любой активный таймер
   - Создает новый TaskTimeLog (end_time = NULL)
   ↓
5. Timer начинает отсчет (0:01, 0:02, 0:03...)
   ↓
6. Пользователь работает над задачей
   ↓
7. Нажимает Stop
   ↓
8. API: POST /tasks/{id}/time/stop
   - Устанавливает end_time = now
   - Вычисляет duration
   ↓
9. Показывает уведомление: "Время остановлено: 2h 30m"
   ↓
10. Время сохранено в истории
    ✅ Можно посмотреть в Time Logs Page
```

---

## 💡 Преимущества:

1. **Простота** - одна кнопка Start/Stop
2. **Автоматизация** - автоматический расчет duration
3. **Защита** - нельзя запустить 2 таймера одновременно
4. **История** - все сессии сохраняются
5. **Статистика** - видно сколько времени на что уходит
6. **Real-time** - таймер обновляется каждую секунду
7. **Красиво** - modern UI с градиентами и анимациями

---

## 🎯 Use Cases:

### UC1: Работа с Pomodoro
```
1. Запустить таймер на 25 минут
2. Работать над задачей
3. Остановить таймер
4. Перерыв 5 минут
5. Повторить
```

### UC2: Отслеживание проектного времени
```
1. Начать работу над задачей → Start
2. Перерыв на обед → Stop
3. Вернулся с обеда → Start (новая сессия)
4. Закончил работу → Stop
5. Итого: 2 сессии, общее время видно в статистике
```

### UC3: Биллинг часов
```
1. Работал над задачей A → 2h 30m
2. Переключился на задачу B → 1h 15m
3. В конце дня смотрю статистику
4. Вижу точное время по каждой задаче
5. Формирую отчет для клиента
```

---

## 🐛 Troubleshooting:

### Проблема: Таймер не запускается

**Решение:**
- Проверьте что миграция применена
- Проверьте логи backend
- Убедитесь что task_id валиден

### Проблема: Время не сохраняется

**Решение:**
- Проверьте что Stop был нажат (end_time установлен)
- Посмотрите логи API вызовов
- Проверьте что duration вычислился

### Проблема: Таймер не обновляется

**Решение:**
- Проверьте что Timer не был disposed
- Убедитесь что setState() вызывается
- Hot restart приложения

---

## 📝 Future Improvements:

- [ ] Timer notifications (напоминания каждые 25 минут)
- [ ] Background timer (работает даже при закрытом приложении)
- [ ] Timer presets (быстрый старт на 25/45/60 минут)
- [ ] Time goals (установка целевого времени на задачу)
- [ ] Charts & Graphs (визуализация статистики)
- [ ] Export reports (экспорт в CSV/PDF)
- [ ] Team time tracking (совместное отслеживание)

---

**Последнее обновление:** 9 декабря 2025  
**Статус:** ✅ Production Ready - Готово к использованию!
