# 📝 Memories Feature - Presentation Layer

Слой представления для функции воспоминаний.

## 📂 Структура

```
presentation/
├── pages/
│   └── create_memory_page.dart    # Страница создания воспоминания
├── widgets/
│   ├── memory_card.dart           # Карточка воспоминания
│   └── widgets.dart               # Экспорт виджетов
└── README.md
```

## 🎨 Компоненты

### CreateMemoryPage

Полнофункциональная страница для создания нового воспоминания.

**Возможности:**
- ✅ Выбор типа контента (текст, ссылка, фото, голос)
- ✅ Форма с валидацией
- ✅ Анимированный UI
- ✅ AI информационная карточка
- ✅ Loading состояние
- ✅ Интеграция с SnackBar уведомлениями

**Использование:**
```dart
Navigator.push(
  context,
  PageTransitions.slideFromBottom(const CreateMemoryPage()),
);
```

**Типы источников:**
- `text` - Текстовая заметка (по умолчанию)
- `link` - Ссылка на внешний ресурс
- `image` - Фотография/изображение
- `voice` - Голосовая заметка

---

### MemoryCard

Красивая карточка для отображения воспоминания в списке.

**Props:**
- `title` - Заголовок воспоминания
- `content` - Основной текст
- `category` - Категория (опционально)
- `tags` - Список тегов (опционально)
- `createdAt` - Дата создания
- `onTap` - Действие при нажатии
- `onEdit` - Действие при редактировании
- `onDelete` - Действие при удалении

**Использование:**
```dart
MemoryCard(
  title: 'Интерстеллар',
  content: 'Фильм Кристофера Нолана про космос...',
  category: 'Movies & TV',
  tags: ['sci-fi', 'nolan', 'space'],
  createdAt: DateTime.now(),
  onTap: () => print('Open memory'),
  onEdit: () => print('Edit memory'),
  onDelete: () => print('Delete memory'),
)
```

**Фичи:**
- 📦 Градиентный фон с glassmorphism
- 🏷️ Отображение категории с градиентом
- 🔖 До 3 тегов с иконками
- ⏰ Умное форматирование времени
- ✏️ Кнопки редактирования и удаления
- 💫 Ripple эффект при нажатии

**Форматы даты:**
- "Только что" - менее минуты назад
- "5 мин назад" - в течение часа
- "3 ч назад" - сегодня
- "Вчера" - вчера
- "5 дн назад" - в течение недели
- "15.03.2024" - более недели назад

---

## 🎯 UI/UX Особенности

### Анимации
- ✅ Slide transition при открытии страницы
- ✅ Fade transition для контента
- ✅ Smooth animation для типов контента
- ✅ Ripple effects для интерактивных элементов

### Дизайн
- 🎨 Современный glassmorphism стиль
- 💎 Градиенты на кнопках и категориях
- 📦 Округлые углы (16-20px)
- 🌈 Цветовое кодирование категорий
- ✨ Тени и блюр эффекты

### Адаптивность
- 📱 Responsive layout
- ⌨️ Keyboard handling
- 🔄 Loading states
- ✅ Form validation
- 🔔 Error handling с SnackBar

---

## 🚀 Следующие шаги

### TODO:
- [ ] Интеграция с backend API
- [ ] Добавить upload изображений
- [ ] Реализовать голосовой ввод
- [ ] Добавить предпросмотр ссылок
- [ ] Создать страницу детального просмотра
- [ ] Добавить редактирование воспоминаний
- [ ] Реализовать удаление с подтверждением
- [ ] Добавить фильтры и сортировку
- [ ] Создать список воспоминаний
- [ ] Добавить поиск

### Планируемые виджеты:
- `MemoryDetailPage` - детальный просмотр
- `MemoryListView` - список воспоминаний
- `MemoryFilter` - фильтр по категориям/тегам
- `MemorySearchBar` - поиск
- `MemoryActions` - bottom sheet с действиями

---

## 💡 Примеры использования

### Создание воспоминания
```dart
// В HomePage или любом другом месте
FloatingActionButton(
  onPressed: () {
    Navigator.push(
      context,
      PageTransitions.slideFromBottom(const CreateMemoryPage()),
    );
  },
  child: Icon(Icons.add),
)
```

### Отображение списка
```dart
ListView.builder(
  padding: EdgeInsets.all(16),
  itemCount: memories.length,
  itemBuilder: (context, index) {
    final memory = memories[index];
    return MemoryCard(
      title: memory.title,
      content: memory.content,
      category: memory.category,
      tags: memory.tags,
      createdAt: memory.createdAt,
      onTap: () => _openMemoryDetail(memory),
      onEdit: () => _editMemory(memory),
      onDelete: () => _confirmDelete(memory),
    );
  },
)
```

---

## 🎨 Кастомизация

### Изменение цветов
Все цвета наследуются из `AppTheme`:
```dart
AppTheme.primaryColor
AppTheme.primaryGradient
AppTheme.surfaceColor
```

### Изменение иконок
Используются Ionicons:
```dart
Ionicons.text_outline      // Текст
Ionicons.link_outline      // Ссылка
Ionicons.image_outline     // Фото
Ionicons.mic_outline       // Голос
```

### Изменение анимаций
Настройка в `initState`:
```dart
_slideController = AnimationController(
  duration: Duration(milliseconds: 600), // Скорость
  vsync: this,
);
```

---

**Все компоненты следуют Clean Architecture и полностью переиспользуемы!** 🎉

