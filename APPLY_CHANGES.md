# 🚀 Как применить изменения

## Что было добавлено сегодня (9 декабря 2025):

1. ✅ Исправлен баг Smart Search
2. ✅ Backend перемещен в проект
3. ✅ Настроены Push Notifications (95%)
4. ✅ Реализованы Task Templates (100%)

---

## 📝 Шаг за шагом:

### 1. Backend - Применить миграцию БД

```bash
cd backend

# Запустить/перезапустить backend
docker-compose up -d

# Применить миграцию для Task Templates
docker-compose exec backend alembic upgrade head

# Должно быть:
# INFO  [alembic.runtime.migration] Running upgrade 267c55c085dc -> a1b2c3d4e5f6, create_task_templates_table
```

### 2. Backend - Загрузить предустановленные шаблоны

```bash
# Загрузить 8 системных шаблонов
docker-compose exec backend python -m app.db.seed_templates

# Вывод:
# 🌱 Seeding system templates...
#   ✅ Created template: Утренняя рутина 🌅
#   ✅ Created template: Вечерняя рутина 🌙
#   ... (всего 8)
# ✅ System templates seeded successfully!
```

### 3. Frontend - Пересобрать приложение

```bash
# Остановить текущее приложение (если запущено)
# В терминале где запущен flutter run: нажать 'q'

# Запустить заново
flutter run

# Или hot restart: нажать 'R'
```

---

## ✅ Проверка что всё работает:

### Smart Search (исправленный баг)
1. Создать воспоминание
2. Нажать "Искать контент"
3. Ввести "Интерстеллар"
4. Выбрать из результатов
5. ✅ Инпуты должны предзаполниться

### Task Templates
1. Открыть Tasks → Create Task (+)
2. Нажать иконку 📋 в AppBar
3. Видеть 8 системных шаблонов
4. Выбрать "🌅 Утренняя рутина"
5. ✅ Задача создана с 3 подзадачами

### Push Notifications (опционально)
Если хотите включить:
1. Получить Firebase Service Account Key
2. Сохранить как `backend/firebase-service-account.json`
3. Добавить в `.env`: `FIREBASE_CREDENTIALS_PATH=./firebase-service-account.json`
4. `docker-compose restart backend`

📘 Инструкция: `backend/QUICK_START_NOTIFICATIONS.md`

---

## 🐛 Если что-то не работает:

### Backend не запускается
```bash
# Проверить логи
docker-compose logs backend

# Пересоздать контейнеры
docker-compose down
docker-compose up -d --build
```

### Миграция не применяется
```bash
# Проверить текущую версию
docker-compose exec backend alembic current

# Должно быть: a1b2c3d4e5f6

# Если нет, применить вручную
docker-compose exec backend alembic upgrade a1b2c3d4e5f6
```

### Шаблоны не загрузились
```bash
# Проверить в БД
docker-compose exec postgres psql -U postgres -d memoir

SELECT COUNT(*) FROM task_templates WHERE is_system = true;
# Должно быть: 8

# Если 0, запустить seeding снова
docker-compose exec backend python -m app.db.seed_templates
```

### Flutter ошибки компиляции
```bash
# Очистить кеш
flutter clean
flutter pub get

# Перегенерировать freezed файлы
flutter pub run build_runner build --delete-conflicting-outputs

# Запустить заново
flutter run
```

---

## 📊 Итог:

После выполнения всех шагов у вас будет:
- ✅ Исправленный Smart Search
- ✅ Backend в правильной структуре проекта
- ✅ 8 готовых к использованию Task Templates
- ✅ Push Notifications (95% готовы)

**Общий прогресс проекта: 98%!** 🎉

---

**Вопросы?** Смотрите документацию в папке `docs/` или `backend/*.md`
